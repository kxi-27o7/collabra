FastAPI backend scaffold for Collabra

Quick start:

- Copy `.env.example` to `.env` and adjust values.
- Build and run with Docker Compose:

```bash
docker compose up --build
```

API docs: http://localhost:8000/docs
