version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: collabra
      POSTGRES_PASSWORD: collabra
      POSTGRES_DB: collabra
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  web:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://collabra:collabra@db:5432/collabra
    depends_on:
      - db

volumes:
  db_data:
